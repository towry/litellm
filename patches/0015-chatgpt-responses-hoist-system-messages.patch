diff --git a/litellm/llms/chatgpt/responses/transformation.py b/litellm/llms/chatgpt/responses/transformation.py
index 8e4bbf1d3c..3ddec34cf1 100644
--- a/litellm/llms/chatgpt/responses/transformation.py
+++ b/litellm/llms/chatgpt/responses/transformation.py
@@ -1,3 +1,4 @@
+from collections.abc import Mapping, Sequence
 from typing import TYPE_CHECKING, Any, Final
 
 from litellm.exceptions import AuthenticationError
@@ -58,6 +59,28 @@ class ChatGPTResponsesAPIConfig(OpenAIResponsesAPIConfig):
         default_headers: Final = get_chatgpt_default_headers(access_token, account_id, session_id)
         return {**default_headers, **headers}
 
+
+    @staticmethod
+    def _system_message_text(item: object) -> str | None:
+        if not isinstance(item, Mapping) or item.get("role") != "system":
+            return None
+
+        content = item.get("content")
+        if isinstance(content, str):
+            return content
+        if not isinstance(content, Sequence):
+            return None
+
+        text_parts = tuple(
+            block.get("text")
+            for block in content
+            if isinstance(block, Mapping)
+            and isinstance(block.get("text"), str)
+        )
+        if len(text_parts) != len(content):
+            return None
+        return "\n".join(text_parts)
+
     def transform_responses_api_request(
         self,
         model: str,
@@ -73,11 +96,33 @@ class ChatGPTResponsesAPIConfig(OpenAIResponsesAPIConfig):
             litellm_params,
             headers,
         )
+        input_items = request.get("input")
+        system_instruction_parts = (
+            tuple(
+                text
+                for item in input_items
+                if (text := self._system_message_text(item)) is not None and text
+            )
+            if isinstance(input_items, list)
+            else ()
+        )
+        if isinstance(input_items, list):
+            request["input"] = [item for item in input_items if self._system_message_text(item) is None]
+
         base_instructions: Final = get_chatgpt_default_instructions()
-        existing_instructions: Final = request.get("instructions")
+        existing_instructions = request.get("instructions")
+        if system_instruction_parts:
+            system_instructions = "\n\n".join(system_instruction_parts)
+            existing_instructions = (
+                f"{existing_instructions}\n\n{system_instructions}"
+                if existing_instructions
+                else system_instructions
+            )
         if existing_instructions:
             if base_instructions not in existing_instructions:
                 request["instructions"] = f"{base_instructions}\n\n{existing_instructions}"
+            else:
+                request["instructions"] = existing_instructions
         else:
             request["instructions"] = base_instructions
         request["store"] = False
