diff --git a/litellm/llms/base_llm/responses/transformation.py b/litellm/llms/base_llm/responses/transformation.py
index b5e1870141..2f349fcb06 100644
--- a/litellm/llms/base_llm/responses/transformation.py
+++ b/litellm/llms/base_llm/responses/transformation.py
@@ -69,6 +69,9 @@ class BaseResponsesAPIConfig(ABC):
     def create_streaming_output_recovery(self) -> ResponsesSSEOutputAccumulator | None:
         return None
 
+    def should_skip_streaming_chunk(self, parsed_chunk: dict[str, object]) -> bool:
+        return False
+
     def sign_request(
         self,
         headers: dict,
diff --git a/litellm/llms/chatgpt/responses/transformation.py b/litellm/llms/chatgpt/responses/transformation.py
index f6a8aced0f..20640a1875 100644
--- a/litellm/llms/chatgpt/responses/transformation.py
+++ b/litellm/llms/chatgpt/responses/transformation.py
@@ -47,6 +47,9 @@ class ChatGPTResponsesAPIConfig(OpenAIResponsesAPIConfig):
     def create_streaming_output_recovery(self) -> ResponsesSSEOutputAccumulator:
         return ResponsesSSEOutputAccumulator()
 
+    def should_skip_streaming_chunk(self, parsed_chunk: dict[str, object]) -> bool:
+        return parsed_chunk.get("type") == "keepalive"
+
     def validate_environment(
         self,
         headers: dict,
diff --git a/litellm/responses/streaming_iterator.py b/litellm/responses/streaming_iterator.py
index 1d71dc1d4e..0f5b51775e 100644
--- a/litellm/responses/streaming_iterator.py
+++ b/litellm/responses/streaming_iterator.py
@@ -362,6 +362,8 @@ class BaseResponsesAPIStreamingIterator:
             if _is_json_object(parsed_chunk):
                 if self.responses_api_provider_config is None:
                     raise ValueError("responses_api_provider_config is required to process live streaming chunks")
+                if self.responses_api_provider_config.should_skip_streaming_chunk(parsed_chunk):
+                    return None
                 recovered_chunk: Final = (
                     self._streaming_output_recovery.process_chunk(parsed_chunk)
                     if self._streaming_output_recovery is not None
